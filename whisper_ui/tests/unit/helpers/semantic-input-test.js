import { semanticInput } from '../../../helpers/semantic-input';
import { module, test } from 'qunit';

module('Unit | Helper | semantic input');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = semanticInput(42);
  assert.ok(result);
});
